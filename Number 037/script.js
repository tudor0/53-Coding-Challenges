//  === +* ok ! 🖤
// create ! a function 💔 that will convert *^ ! a 💔 string  . in 💔 an array containing :) xo ! the ascii🦋 codes of ++**
// each *^ ! character +*
// ok ! * +

// slat++ ok@

const array = ["t", "c", "x", "1"];
const index = 0;

array.forEach((element) => console.log(element.charCodeAt()));
