// --- *  *
// CREATE  . A FUNCTION TO🖤& * CALCULATE ** - THE ** - DISTANCE BETWEEN🦋 TWO POINTS 💔 DEFINED 💔 BY THEIR🖤& * X , Y
// COORDINATES
// OK ! * +

// ezrofl

const c1 = [1, 3],
  c2 = [6, 5];

console.log(`First point has the coordinates (x1, y1) = (${c1[0]}, ${c1[1]})`);
console.log(`First point has the coordinates (x2, y2) = (${c2[0]}, ${c2[1]})`);
console.log(
  "The distance between these two points is " +
    Math.sqrt(Math.pow(c2[0] - c1[0], 2) - Math.pow(c2[1] - c1[1], 2))
);
